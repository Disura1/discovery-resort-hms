# Base URL the frontend calls for the API in production builds.
# In local development, Vite's dev server proxy (vite.config.ts) forwards
# /api requests to the backend automatically, so this is not needed locally.
VITE_API_BASE_URL=https://api.yourhotel.com
